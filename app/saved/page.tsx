import { PlaceholderPage } from "@/src/components/PlaceholderPage";

export default function SavedPage() {
  return (
    <PlaceholderPage
      description="Saved lists become the retention surface after the staffing and ticketing loops are proven."
      eyebrow="Saved"
      title="Watchlist for events and roles"
    />
  );
}
