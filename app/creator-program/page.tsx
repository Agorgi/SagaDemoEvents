import { PlaceholderPage } from "@/src/components/PlaceholderPage";

export default function CreatorProgramPage() {
  return (
    <PlaceholderPage
      description="Creator Program is stubbed here because the core demo focuses on the host-to-crew staffing loop."
      eyebrow="Creator Program"
      title="Scale distribution through creator partners"
    />
  );
}
